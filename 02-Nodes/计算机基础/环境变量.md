---
tags:
  - 计算机
---

环境变量的地址电脑会直接查询，可以在任意地址下访问环境变量中的地址
比如我们把某个深入的文件夹如 `/AVs` 地址加入环境变量，这个文件夹存放了如 ` aaa.mp4 ` 这种视频

如果没有加入，在 cmd 中输入 `aaa.mp4` 会报错，但加入环境变量后，就可以直接访问

所以这就是为什么加入 python，java 环境都要添加环境变量，这是为了确保在你的项目目录下也可以直接运行