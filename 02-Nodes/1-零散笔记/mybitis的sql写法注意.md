---
tags:
  - 计算机/后端/springboot
---

对于寻常预编译 sql，我们使用#{} 作为占位符进行参数注入，

但当我们使用如   **like '#{}'** 时候会报错
这是因为，在#{}被替换为? 时，由于? 被单引号包裹，数据库不会认为这是一个占位符，而识别为普通符号


此时需要使用 *CONCAT ('%', #{name}, '%')*，拼接语句，此时可以正常