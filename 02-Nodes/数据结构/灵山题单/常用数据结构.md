---
tags:
  - 算法
---

# 0 常用枚举思路

## 0.1 枚举右，维护左

问题：有一个目标 T ，求一个列表中是否存在 a+b=T 的 ab 和下标

将问题转化为 a=T-b。
找到 b，检查他前面是否有 a 满足

使用哈希表可以实现，遍历到当前 b，先查哈希表里有没有 T−b 也就是 a；查完再把当前 b 存入哈希表
只需要一次遍历

时间复杂度：O(n)
空间复杂度：O(n)

扩展：
	不存在所谓 T，如极值，最大差值问题
	不用哈希表，只用不断更新变量状态就行