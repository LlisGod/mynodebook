---
tags:
  - 计算机/SQL
---

# 外键
## 物理外键
直接在数据库层面为两张表添加外键关联
可以保证数据库的一致性和完整性

## 逻辑外键

在服务层面定义处理两张表的关联，保证数据一致性和完整性
而在数据库层面表间独立

物理外键会极大限制数据库的性能
现在企业多使用逻辑外键



### 一对一关系
只需给外键对应字段添加唯一标识即可



## 多对多关系
需要创建一个中间表，存对应主键就好
比如：学生表->学生课程表<-课程表